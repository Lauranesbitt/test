<%= form_with(model: hospital, local: true) do |form| %>
  <% if hospital.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hospital.errors.count, "error") %> prohibited this hospital from being saved:</h2>

      <ul>
      <% hospital.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hospitalname %>
    <%= form.text_field :hospitalname, id: :hospital_hospitalname %>
  </div>

  <div class="field">
    <%= form.label :hospitaladdress %>
    <%= form.text_field :hospitaladdress, id: :hospital_hospitaladdress %>
  </div>

  <div class="field">
    <%= form.label :hospitalnumber %>
    <%= form.text_field :hospitalnumber, id: :hospital_hospitalnumber %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
